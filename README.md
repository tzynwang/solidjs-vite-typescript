# SolidJS-Vite-TypeScript

## Quick start

```shell
cp .env.example .env
make i
make start
# then open http://localhost:3000
```

## Deploy

```shell
make build
make preview
# then open http://localhost:8080
```
